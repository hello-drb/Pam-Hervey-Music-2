---
import { siteSettings } from '../data/siteContent';
---

<div class="overflow-hidden bg-warm-black py-3">
  <div class="animate-marquee whitespace-nowrap flex">
    {Array(10).fill(null).map(() => (
      <span class="text-cream text-sm tracking-[0.3em] uppercase font-light mx-8">
        Follow Pam On Social —
      </span>
    ))}
  </div>
</div>
